apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: fullstack
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullSecrets:
        - name: gitlab-registry
      containers:
      - name: backend
        image: ghcr.io/abdo-salem9982/devops-lab/backend:dc35c0083c9b322fc43e52870da1b44ed99b8dd1
        ports:
        - containerPort: 5000
        envFrom:
        - configMapRef:
            name: backend-configmap
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
